import 'package:flutter/material.dart';

class VendorOrderDetailPage extends StatelessWidget {
  const VendorOrderDetailPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Detalhe do Pedido')),
      body: Center(child: Text('Detalhe do pedido (em breve)')),
    );
  }
} 